# Module 7 — Selector Validation

Module 7 introduces selector manifests and live assertion execution.

Delivered:

- JSON and YAML manifests with schema validation.
- Required and optional selector semantics.
- Exact and ranged count assertions.
- Visibility, enabled, and editable assertions.
- Main-frame and child-frame validation.
- Structured result and summary types.
- Versioned JSON reports.
- Exit code 0 for required success, 1 for required failures, and 2 for invalid input.
- Real Chromium smoke tests covering optional and required failures.

Suggested commit:

```bash
git add -A
git commit -m "Add selector manifest validation"
```
