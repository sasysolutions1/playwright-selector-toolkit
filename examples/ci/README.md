# CI templates

Copy `selector-health.yml` to `.github/workflows/selector-health.yml` and adjust the URL, plugin,
manifest, schedule, and secret names. The example uploads artifacts on success or failure.
